<div class="container">
    <div class="row whitebg">    
    <div class="twelve column">
    <h1>Raider News</h1>
    
    <ul>
        <% @news_items.each do |news_item| %>
            <li>
                <h5><%= link_to news_item.title, news_item.link %><small><%= news_item.pub_date %></small>
                    <%= link_to 'Destroy', news_item, method: :delete, data: { confirm: 'Are you sure?' } %>
                    <%= link_to 'Edit', edit_news_item_path(news_item) %> </h5>
                <p><%= news_item.short_desc %><p>
            </li>
        <% end %>
    </ul>
    <%= paginate @news_items %>
    </div>
</div>
</div>

    <%#= link_to 'Show', news_item %>
    <%#= link_to 'Edit', edit_news_item_path(news_item) %> 
    <%#= link_to 'Destroy', news_item, method: :delete, data: { confirm: 'Are you sure?' } %>
    


        

<%#= link_to 'New News item', new_news_item_path %>