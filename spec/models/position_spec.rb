require 'spec_helper'

describe Position do
  # let(:player) { FactoryGirl.create(:player) }

  # before { @position = player.positions.build(player_position: "QB") }

  # subject { @position }

  # it { should respond_to(:player_position) }
  # it { should respond_to(:player_id) }
  # it { should respond_to(:player) }
  # its(:player) { should == player }

  # it { should be_valid }

  # describe "accessible attributes" do
  # 	it "should not allow access to player_id" do
  # 		expect do
  # 			Position.new(player_id: player.id)
  # 		end.should raise_error(ActiveModel::MassAssignmentSecurity::Error)
  # 	end
  # end

  # describe "when player_id is not present" do
  # 	before { @position.player_id = nil }
  # 	it { should_not be_valid }
  # end
end